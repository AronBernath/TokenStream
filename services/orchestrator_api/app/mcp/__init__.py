"""
MCP (Model Context Protocol) integration.

This package provides:
- Remote MCP clients over Streamable HTTP (preferred) with legacy SSE fallback
- Multi-server tool registry with namespacing
- Tool execution helpers (convert MCP tool results to LLM tool messages)
"""
